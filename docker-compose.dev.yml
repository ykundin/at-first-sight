version: "3.3"
services:
  db:
    image: mongo:5.0.2
    restart: always
    environment:
      - MONGO_INITDB_ROOT_USERNAME=kundin
      - MONGO_INITDB_ROOT_PASSWORD=very-secret-password
      - MONGODB_DATABASE=at-first-sight
    ports:
      - 27017:27017
    volumes:
      - db_data:/data/db
      - ./mongo/docker-entrypoint-initdb.d:/docker-entrypoint-initdb.d:ro

  object-storage:
    image: bitnami/minio:latest
    restart: always
    environment:
      - MINIO_ROOT_USER=kundin
      - MINIO_ROOT_PASSWORD=very-secret-password
      - MINIO_SKIP_CLIENT=yes
    ports:
      - 9000:9000
      - 9001:9001
    volumes:
      - minio_storage:/data

  imgproxy:
    image: darthsim/imgproxy
    environment:
      - IMGPROXY_USE_S3=true
      - IMGPROXY_S3_ENDPOINT=http://object-storage:9000
      - AWS_ACCESS_KEY_ID=Rh5Vl03ij4oUg0nHpVQv
      - AWS_SECRET_ACCESS_KEY=fw1Q6vGzB0JgGqo7fEeUQNlSxupU1c0uQ9Vthvwi

  backend:
    image: oven/bun:1.0.4
    restart: always
    working_dir: /app
    ports:
      - 4000:4000
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - PORT=4000
      - OBJECT_STORAGE_ENDPOINT=http://object-storage:9000
      - OBJECT_STORAGE_BUCKET=at-first-sight
      - OBJECT_STORAGE_ACCESS_KEY=Rh5Vl03ij4oUg0nHpVQv
      - OBJECT_STORAGE_SECRET_KEY=fw1Q6vGzB0JgGqo7fEeUQNlSxupU1c0uQ9Vthvwi
    command: bun run dev

  tg-web-app:
    image: oven/bun:1.0.4-alpine
    restart: always
    working_dir: /app
    ports:
      - 5173:5173
    volumes:
      - ./tg-web-app:/app
    environment:
      - PORT=5173
      - BACKEND_URL=http://backend:4000
      - IMGPROXY_URL=http://imgproxy:8080
      - OBJECT_STORAGE_BUCKET=at-first-sight
    command: bun run dev

volumes:
  db_data:
  minio_storage:
